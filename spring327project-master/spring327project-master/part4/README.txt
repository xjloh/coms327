1. project4.c
	-display the interface
	-display game configuration such as foundation, waste, stock, and tableau.
	-perform moves made by user such as quit, restart, next card, reset stock and so on.
	- stops when an invalid move is made

2. pt4utils.h
	-contains functions brought over from part3 with additional functions such as validateMove() and executeMove().

3. pt4utils.c
	-contains functions brought over from part3 with additional functions such as validateMove() and executeMove().

4. Makefile
	-build new executables called game along with the other executables.
