1. pt3utils.c
	-brought over from part2
	-inproved functions like getAllData that reads data from input file.

2. pt3utils.h
	- added new struct type called Config that contains all the data neccesary from a configuration of a game.

3. Project3.c 
	- fulfilling all kinds of infile. 
	- implementing the move method that loops through all moves in the linked list
	- foundation card's rank checker before putting card into foundation 
	- tableau card's rank and suit checker before moving the card from one column to another
	- check the current configuration is winnable or not
	- get the next possible moves
	- get all the origin moves
	- validate the moves and check if its valid move
	
